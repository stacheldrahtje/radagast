#include <iostream>
#include <string>
#include <fstream>

bool std::check_users (string uname)
{
  std::fstream file_object;
  std::file_object.open ("/etc/passwd", ios::in);
  std::string line_read;
  while (file_object, line_read)
  {
    
  }

//check_users file
